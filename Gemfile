source 'https://rubygems.org'
ruby '3.1.0'

gem 'jekyll', '~> 4.3'

# GitHub Pages compatibility
gem 'github-pages', group: :jekyll_plugins

# Plugins used by the site (from _config.yml)
gem 'jekyll-feed'
gem 'jekyll-paginate'

# Development dependencies
group :development do
  gem 'webrick'
end

